# Global search

**File:** `global-search-post-result.png`

AppShell search with query `legend`:

- Post result card: thumbnail, title, “Post by …”
- Same header chrome as other pages

**Web:** search in `AppShell` / feed integration

**Mobile:** `mobile/app/search.tsx`
