# Create compare

**File:** `create-compare-whats-your-take.png`

New post flow:

- Two option upload boxes (URL or label), **VS**, “+ Add option”
- **Category** (required), **Campaign** (optional), **Caption**
- **Voting deadline** toggle
- **Launch It** / **Schedule** / Cancel
- Bottom nav: Create tab active (purple)

**Web:** `src/pages/CreatePostPage.tsx`
